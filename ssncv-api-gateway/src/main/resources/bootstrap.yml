spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ssncv-config-server
      profile: dev
      name: ssncv
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://zaqgogogo1:1111/eureka/, http://zaqgogogo2:1112/eureka/
debug: true